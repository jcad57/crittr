/**
 * Temporary: hide Google OAuth on sign-in / sign-up while keeping
 * `signInWithGoogle` / `lib/auth/googleOAuth` wired for a quick revert.
 * Flip to `true` when slow OAuth is resolved.
 */
export const SHOW_GOOGLE_AUTH_ON_EMAIL_SCREENS = false;
